spring.application.name=SK Bazaar

# Server Configuration
server.port=${PORT:8080}

# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://bkdidir94vhidbpch7ox-mysql.services.clever-cloud.com:3306/bkdidir94vhidbpch7ox?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:ujcoipqkp3ovweit}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:ZpWdaK1qLTASMzMCuuIG}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hikari Connection Pool Configuration (Clever Cloud max_user_connections limit is 5)
# Set to 1 to allow room for DDL connections and rolling updates on Render
spring.datasource.hikari.maximum-pool-size=1
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Spring AI Configuration
spring.ai.google.genai.api-key=${SPRING_AI_GOOGLE_GENAI_API_KEY:}
spring.ai.google.genai.project-id=${SPRING_AI_GOOGLE_PROJECT_ID:unused}
spring.ai.google.genai.location=${SPRING_AI_GOOGLE_LOCATION:us-central1}
